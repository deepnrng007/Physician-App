export type contactType = {
  userId: string;
  recordID: number;
  firstName: string;
  lastName: string;
  thumbnailPath: string;
  phoneNumber: string;
  description: string;
  contactType: string;
};
